{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "seed:shop": "tsx ./src/seed-stripe-products.ts",
    "seed:product-costs": "tsx ./src/seed-product-costs.ts",
    "auth:bootstrap-admin": "tsx ./src/auth-bootstrap-admin.ts",
    "auth:set-admin-password": "tsx ./src/auth-set-admin-password.ts",
    "preflight:prod": "tsx ./src/preflight-prod-env.ts",
    "verify:deploy": "tsx ./src/verify-deploy.ts",
    "check:schema-drift": "tsx ./src/check-schema-drift.ts",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --passWithNoTests --coverage",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "vitest": "^4.1.6"
  },
  "dependencies": {
    "@workspace/resupply-auth": "workspace:*",
    "@workspace/resupply-db": "workspace:*",
    "@workspace/resupply-email": "workspace:*",
    "stripe": "^22.1.1"
  }
}

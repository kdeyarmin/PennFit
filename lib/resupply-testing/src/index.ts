// @workspace/resupply-testing
// Test fixtures, factories, and mock vendor implementations. Anything reusable across test suites lives here so the api / worker / domain test suites stay slim. devDeps only — never imported by production code.
//
// Phase 0 — scaffold only. No business logic yet.

export {};

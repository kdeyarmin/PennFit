openapi: 3.1.0
info:
  # Do not change the title — orval uses it to name generated output files.
  title: Api
  version: 0.1.0
  description: |
    CPAP Resupply Automation operator API. Powers the operator dashboard
    (queue, episode detail, conversation viewer, fulfillment). Patient
    PHI flows through this API and lives in the resupply.* schema.
servers:
  - url: /resupply-api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: operator
    description: Operator identity & session
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Liveness check
      description: |
        Liveness probe. Does NOT touch the database or any other
        dependency. Use /readyz for readiness gating.
      responses:
        "200":
          description: Process is up
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /readyz:
    get:
      operationId: readinessCheck
      tags: [health]
      summary: Readiness check
      description: |
        Readiness probe. Returns 200 only when every dependency the API
        needs to serve traffic (Postgres + the pg-boss queue) is
        reachable. The deploy gate uses this to decide whether the
        service is healthy enough to receive traffic. Failure responses
        report which dependency failed without leaking connection
        details — see CheckError for the allowed failure categories.
      responses:
        "200":
          description: All dependencies reachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessStatus"
        "503":
          description: One or more dependencies unreachable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReadinessStatus"
  /me:
    get:
      operationId: getOperatorMe
      tags: [operator]
      summary: Operator identity
      description: |
        Returns the signed-in operator's Clerk id and verified primary
        email when the caller is on the `RESUPPLY_OPERATOR_EMAILS`
        allowlist. The dashboard calls this immediately after sign-in
        to decide whether to render the operator console (200), the
        "not authorized" screen (403), or the "allowlist not
        configured" screen (503 in production with the env var unset).
      responses:
        "200":
          description: Caller is a recognized operator
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorIdentity"
        "401":
          description: |
            No valid Clerk session, or Clerk lookup failed. The body
            contains a generic error message — never the underlying
            driver text.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthError"
        "403":
          description: |
            Signed in, but either the primary email is unverified or
            the email is not on the operator allowlist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthError"
        "503":
          description: |
            Operator allowlist is not configured on this server (only
            possible in production — dev falls back to allowing any
            signed-in, email-verified user).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthError"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
          enum: [ok]
        service:
          type: string
          enum: [resupply-api]
      required:
        - status
        - service
    OperatorIdentity:
      type: object
      description: |
        Minimal identity payload for an authorized operator. Contains
        ONLY the fields the dashboard needs to render — no session
        token, no Clerk user object, no allowlist contents.
      properties:
        clerkId:
          type: string
          description: Stable Clerk user id, used for audit log writes.
        email:
          type: string
          format: email
          description: |
            Verified primary email, lowercased. Displayed in the
            dashboard chrome and recorded in audit rows.
      required:
        - clerkId
        - email
    AuthError:
      type: object
      description: |
        Auth failure body. Carries a single human-readable `error`
        string safe for the dashboard to render verbatim. Never
        contains the underlying error message, the user id, or any
        connection details.
      properties:
        error:
          type: string
      required:
        - error
    CheckStatus:
      type: string
      enum: [ok, failed]
    CheckError:
      type: string
      description: |
        Closed set of failure categories. We never expose raw driver
        error text — only one of these labels.
      enum:
        - timeout
        - connection_refused
        - host_not_found
        - schema_not_initialized
        - database_starting_up
        - database_does_not_exist
        - unavailable
    ReadinessStatus:
      type: object
      properties:
        status:
          type: string
          enum: [ready, not_ready]
        checks:
          type: object
          properties:
            db:
              $ref: "#/components/schemas/CheckStatus"
            queue:
              $ref: "#/components/schemas/CheckStatus"
          required: [db, queue]
        errors:
          type: object
          description: |
            Per-dependency failure category. Only present when status
            is not_ready, and only includes entries for dependencies
            whose check failed.
          properties:
            db:
              $ref: "#/components/schemas/CheckError"
            queue:
              $ref: "#/components/schemas/CheckError"
      required:
        - status
        - checks

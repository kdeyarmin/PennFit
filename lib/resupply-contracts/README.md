# @workspace/resupply-contracts

Shared Zod schemas + DTOs used by the api server, the worker, and the operator dashboard. Depends only on zod. The single source of truth for over-the-wire shapes.

## Status

Phase 0 — scaffolding only. No exports yet. See `docs/resupply/adr/` for the
architectural decisions that govern this package.
